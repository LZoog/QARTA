{
  "name": "qarta",
  "version": "1.0.0",
  "description": "",
  "main": "app-complied.js",
  "scripts": {
    "start": "babel app.js --out-file app-compiled.js --source-maps inline",
    "test": "echo \"Error: no test specified\" && exit 1",
    "electron": "yarn start && electron ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "blink-diff": "^1.0.13",
    "imageoptim": "^0.5.0",
    "nightmare": "^3.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
